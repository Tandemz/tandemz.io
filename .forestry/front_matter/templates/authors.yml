---
label: Authors
hide_body: false
fields:
- name: list
  type: field_group_list
  fields:
  - name: firstName
    type: text
    config:
      required: true
    label: Prénom
  - name: lastName
    type: text
    config:
      required: true
    label: Nom
    description: Nom
  - name: avatar
    type: file
    config:
      maxSize: 250
    label: Avatar
    description: Avatar
  config:
    min: 
    max: 
    labelField: firstName
  label: Auteurs
  description: List of the authors of the blog
pages:
- src/data/authors.json
